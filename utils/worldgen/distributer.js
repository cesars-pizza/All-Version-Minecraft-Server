const {Socket} = require('../../data_structures')

/** 
 * @param {Socket} socket 
 */
function GenerateBlocks(socket) {
    if (socket.upvn == -1) return require('./29').GenerateBlocks
}

module.exports = {GenerateBlocks}