const {Socket} = require('../../data_structures')

/** 
 * @param {Socket} socket 
 */
function Disconnect(socket) {
    if (socket.upvn == -1) return require('./29').Disconnect
}

module.exports = {Disconnect}